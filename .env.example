# Optional. With a key, Decline Doctor uses Claude for classification + message drafting.
# Without it, the app runs a fully deterministic diagnosis from the taxonomy (still works).
ANTHROPIC_API_KEY=
